{
  "name": "@flatwrite/mcp-render-server",
  "version": "0.1.0",
  "description": "MCP server exposing FlatWrite's render API as two MCP tools.",
  "main": "dist/index.js",
  "type": "module",
  "license": "AGPL-3.0",
  "private": true,
  "bin": {
    "flatwrite-mcp-render": "dist/index.js"
  },
  "scripts": {
    "dev": "tsx src/index.ts",
    "build": "tsc -p tsconfig.json && node -e \"const fs=require('fs'); fs.copyFileSync('dist/shared/mcpShared.js', '../../public/webmcp-shared.js');\" && node scripts/build-manifest.mjs",
    "start": "node dist/index.js",
    "lint": "tsc -p tsconfig.json --noEmit",
    "test": "bun test test/"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.4"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "bun-types": "^1.3.14",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0"
  },
  "engines": {
    "node": ">=18"
  }
}
