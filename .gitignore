node_modules/
test-results/
playwright-report/
.playwright-mcp/
bun.lock
.vercel
.env*.local
.dev.vars
.dev.vars*
!.dev.vars.example
!.env.example
.DS_Store
.kilo/
.qodo/
.devin/
dist/

# Python venvs and lockfiles (services/extract/)
.venv/
__pycache__/
*.pyc
# uv.lock is gitignored globally because most JS projects in this repo
# don't want it. The Python service under services/extract/ DOES need its
# lockfile for reproducible Docker builds — that path is whitelisted below.
uv.lock
!/services/extract/uv.lock

# local preview artifacts from manual render tests
preview/

# wrangler files
.wrangler

# Bruno local credentials and disposable OpenAPI importer output
bruno/*/environments/Local.bru
bruno/.openapi-import/
bruno/results/
