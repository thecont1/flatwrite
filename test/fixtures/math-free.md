# Math-free document

This fixture must never trigger the Math Mode nudge.

- Price list: $5, $10, $100
- Windows path-like text: C:\Users\home\file.txt
- Shell: `echo $HOME` and `$PATH`
- Code:

```js
const price = "$19.99";
const path = "C:\\temp\\file";
```

Regular **markdown** only.
