{
  "name": "flatwrite-mcp",
  "version": "0.1.0",
  "description": "MCP server with Streamable HTTP transport, exposed at mcp.flatwrite.md/mcp. Fronts the same render.flatwrite.md Worker as the public HTTP API and the MCP stdio server.",
  "private": true,
  "type": "module",
  "main": "src/index.js",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0",
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "wrangler": "^3.99.0"
  }
}
