meta {
  name: WebMCP create_share_link (browser side effect) (browser only)
  type: http
  seq: 18
  tags: [webmcp, browser-only, webmcp-side-effect]
  webmcp-type: webmcp-side-effect
}

post {
  url: webmcp://document.modelContext/create-share-link-stub
  body: none
  auth: none
}

script:pre-request {
  bru.runner.skipRequest();
}

docs {
  Browser-only WebMCP contract. This tool manipulates the FlatWrite editor via
  document.modelContext and has no HTTP surface. Bruno CLI skips this request.

  Input: {}
  Output: { ok, documentId, shareUrl, expiresAt? }
}
